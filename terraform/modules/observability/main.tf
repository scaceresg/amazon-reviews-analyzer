# Skeleton: observability with CloudWatch.
# Resources to define:
#   - aws_cloudwatch_log_group.* (batch, glue, step functions)
#   - aws_cloudwatch_metric_alarm.* (pipeline failures, duration, cost)
